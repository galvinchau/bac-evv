name: Test API Only

on:
  workflow_dispatch: # luôn bật nút "Run workflow" thủ công
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  api-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: true

      - name: Lint API
        run: pnpm lint

      - name: Typecheck API
        run: pnpm typecheck

      - name: Build API
        run: pnpm build
